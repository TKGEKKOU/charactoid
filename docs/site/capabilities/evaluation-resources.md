# 评测与系统资源

## 评测

评测模块用于把知识库或 Agent 的实际输出收集、执行、比较和导出。典型过程是：

```text
准备评测数据集
→ 生成或同步候选题
→ 执行评测运行
→ 查看单题与汇总结果
→ 分析失败样本
→ 导出历史
```

## 系统资源

资源管理页负责检查和准备 FFmpeg、ASR、Embedding、Reranker、Separator、RVC 和 GPT-SoVITS 等可选组件。

资源状态要区分：未配置、未安装、安装中、已安装未启动、运行中、失败和可用。

> “安装成功”不等于“任务可执行”：还需要检查模型目录、设备、服务探针和权限。
