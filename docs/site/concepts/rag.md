# RAG 与知识资源

RAG 在 CHARACTOID 中不是独立聊天机器人，而是角色 Agent 的一个可调用知识能力：

1. 摄取 Markdown、PDF、网页或项目文档；
2. 清洗并按语义边界切分；
3. 写入向量索引，同时保留来源和段落位置；
4. 对当前问题生成检索请求；
5. 返回带来源的片段，交给 Agent 组织回答；
6. 在会话中保留引用，便于复核和再次检索。

检索结果应当能回到原始资料，不能只返回没有来源的相似度文本。
